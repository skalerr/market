@model List<Provider>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

@foreach (var item in Model)
{
    
    <div>
        <p>
            @item.Id - ID
        </p>
        <p>
            @item.Name - Name
        </p>
    </div>
}
